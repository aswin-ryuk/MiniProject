<% title = t('common.new',model_name: Invoice.model_name.human) %>

<%= render partial: 'shared/display_title_btns', locals: {title: title} %>

<%= simple_form_for @invoice, wrapper: :horizontal_form, html: { autocomplete: "off", :class => 'form-horizontal custom-form' } do |f| %>
	<%= render partial: 'form', locals: {f: f} %>
<% end %>
