 <% title = t('common.edit',model_name: Course.model_name.human) %>

<%= render partial: 'shared/display_title_btns', locals: {title: title, back: true} %>

<%= simple_form_for @course, wrapper: :horizontal_form, html: {:class => 'form-horizontal custom-form' } do |f| %>
	<%= render partial: 'form', locals: {f: f} %>
<% end %>
